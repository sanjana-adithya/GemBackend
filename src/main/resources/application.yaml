server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:mem:gemdb
    driver-class-name: org.h2.Driver
    username: admin
    password:

  h2:
    console:
      enabled: true
    path: /h2-console

  jpa:
    hibernate:
      dbl-auto: create-drop
    show-sql: true


